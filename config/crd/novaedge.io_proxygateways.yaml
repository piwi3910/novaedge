---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: proxygateways.novaedge.io
spec:
  group: novaedge.io
  names:
    kind: ProxyGateway
    listKind: ProxyGatewayList
    plural: proxygateways
    singular: proxygateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.vipRef
      name: VIP Ref
      type: string
    - jsonPath: .spec.ingressClassName
      name: Ingress Class
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProxyGateway defines listeners, TLS configuration, and ingress
          class
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProxyGatewaySpec defines the desired state of ProxyGateway
            properties:
              ingressClassName:
                description: IngressClassName is the ingress class name for Ingress
                  resource compatibility
                type: string
              listeners:
                description: Listeners define the ports and protocols this gateway
                  accepts
                items:
                  description: Listener defines a port and protocol to listen on
                  properties:
                    hostnames:
                      description: Hostnames is a list of hostnames this listener
                        accepts (for HTTP/HTTPS)
                      items:
                        type: string
                      type: array
                    name:
                      description: Name is a unique name for this listener
                      maxLength: 253
                      minLength: 1
                      type: string
                    port:
                      description: Port is the network port to listen on
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    protocol:
                      description: Protocol is the application protocol
                      enum:
                      - HTTP
                      - HTTPS
                      - TCP
                      - TLS
                      type: string
                    tls:
                      description: TLS contains TLS configuration (required for HTTPS/TLS
                        protocols)
                      properties:
                        cipherSuites:
                          description: CipherSuites is a list of allowed cipher suites
                          items:
                            type: string
                          type: array
                        minVersion:
                          description: 'MinVersion is the minimum TLS version (default:
                            TLS 1.2)'
                          enum:
                          - TLS1.2
                          - TLS1.3
                          type: string
                        secretRef:
                          description: SecretRef references a Kubernetes Secret containing
                            TLS certificate and key
                          properties:
                            name:
                              description: name is unique within a namespace to reference
                                a secret resource.
                              type: string
                            namespace:
                              description: namespace defines the space within which
                                the secret name must be unique.
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - secretRef
                      type: object
                  required:
                  - name
                  - port
                  - protocol
                  type: object
                minItems: 1
                type: array
              vipRef:
                description: VIPRef references the ProxyVIP to use for this gateway
                type: string
            required:
            - listeners
            - vipRef
            type: object
          status:
            description: ProxyGatewayStatus defines the observed state of ProxyGateway
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the gateway's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              listenerStatus:
                description: ListenerStatus contains status for each listener
                items:
                  description: ListenerStatus contains status for a single listener
                  properties:
                    name:
                      description: Name matches the listener name
                      type: string
                    ready:
                      description: Ready indicates if the listener is ready to accept
                        traffic
                      type: boolean
                    reason:
                      description: Reason provides detail about the listener status
                      type: string
                  required:
                  - name
                  - ready
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
