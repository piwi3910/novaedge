---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: proxyroutes.novaedge.io
spec:
  group: novaedge.io
  names:
    kind: ProxyRoute
    listKind: ProxyRouteList
    plural: proxyroutes
    singular: proxyroute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.hostnames
      name: Hostnames
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProxyRoute defines routing rules for HTTP requests
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProxyRouteSpec defines the desired state of ProxyRoute
            properties:
              hostnames:
                description: Hostnames defines the hostnames for which this route
                  applies
                items:
                  type: string
                type: array
              rules:
                description: Rules are a list of HTTP routing rules
                items:
                  description: HTTPRouteRule defines semantics for matching an HTTP
                    request and routing it
                  properties:
                    backendRef:
                      description: BackendRef references the backend to route to
                      properties:
                        name:
                          description: Name is the name of the ProxyBackend
                          type: string
                        namespace:
                          description: Namespace is the namespace of the ProxyBackend
                            (defaults to route namespace)
                          type: string
                        weight:
                          default: 1
                          description: Weight defines the proportion of requests sent
                            to this backend
                          format: int32
                          minimum: 0
                          type: integer
                      required:
                      - name
                      type: object
                    filters:
                      description: Filters define processing steps that must be completed
                        during the request or response lifecycle
                      items:
                        description: HTTPRouteFilter defines processing steps that
                          must be completed during the request or response lifecycle
                        properties:
                          add:
                            description: Add contains headers to add (for AddHeader
                              type)
                            items:
                              description: HTTPHeader represents an HTTP header name
                                and value
                              properties:
                                name:
                                  description: Name is the name of the header
                                  minLength: 1
                                  type: string
                                value:
                                  description: Value is the value of the header
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          redirectURL:
                            description: RedirectURL is the URL to redirect to (for
                              RequestRedirect type)
                            type: string
                          remove:
                            description: Remove contains header names to remove (for
                              RemoveHeader type)
                            items:
                              type: string
                            type: array
                          rewritePath:
                            description: RewritePath is the path to rewrite to (for
                              URLRewrite type)
                            type: string
                          type:
                            description: Type identifies the type of filter to apply
                            enum:
                            - AddHeader
                            - RemoveHeader
                            - RequestRedirect
                            - URLRewrite
                            type: string
                        required:
                        - type
                        type: object
                      maxItems: 16
                      type: array
                    matches:
                      description: Matches define conditions used for matching the
                        rule against incoming requests
                      items:
                        description: HTTPRouteMatch defines a match for an HTTP request
                        properties:
                          headers:
                            description: Headers specifies HTTP request header matchers
                            items:
                              description: HTTPHeaderMatch describes how to match
                                an HTTP header
                              properties:
                                name:
                                  description: Name is the name of the HTTP header
                                  minLength: 1
                                  type: string
                                type:
                                  default: Exact
                                  description: Type specifies how to match against
                                    the header value
                                  enum:
                                  - Exact
                                  - RegularExpression
                                  type: string
                                value:
                                  description: Value is the value of the header
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            maxItems: 16
                            type: array
                          method:
                            description: Method matches the HTTP method
                            enum:
                            - GET
                            - HEAD
                            - POST
                            - PUT
                            - PATCH
                            - DELETE
                            - CONNECT
                            - OPTIONS
                            - TRACE
                            type: string
                          path:
                            description: Path specifies a HTTP request path matcher
                            properties:
                              type:
                                description: Type specifies how to match against the
                                  path value
                                enum:
                                - Exact
                                - PathPrefix
                                - RegularExpression
                                type: string
                              value:
                                description: Value is the path to match
                                minLength: 1
                                type: string
                            required:
                            - type
                            - value
                            type: object
                        type: object
                      maxItems: 8
                      type: array
                  required:
                  - backendRef
                  type: object
                maxItems: 16
                minItems: 1
                type: array
            required:
            - rules
            type: object
          status:
            description: ProxyRouteStatus defines the observed state of ProxyRoute
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the route's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
