---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: proxypolicies.novaedge.io
spec:
  group: novaedge.io
  names:
    kind: ProxyPolicy
    listKind: ProxyPolicyList
    plural: proxypolicies
    singular: proxypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.targetRef.kind
      name: Target Kind
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target Name
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProxyPolicy defines authentication, rate-limiting, and other
          policies
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProxyPolicySpec defines the desired state of ProxyPolicy
            properties:
              cors:
                description: CORS configuration (for CORS type)
                properties:
                  allowCredentials:
                    description: AllowCredentials indicates whether credentials are
                      allowed
                    type: boolean
                  allowHeaders:
                    description: AllowHeaders is a list of allowed headers
                    items:
                      type: string
                    type: array
                  allowMethods:
                    description: AllowMethods is a list of allowed HTTP methods
                    items:
                      type: string
                    type: array
                  allowOrigins:
                    description: AllowOrigins is a list of allowed origins
                    items:
                      type: string
                    type: array
                  exposeHeaders:
                    description: ExposeHeaders is a list of headers to expose
                    items:
                      type: string
                    type: array
                  maxAge:
                    description: MaxAge is how long the response to a preflight request
                      can be cached
                    type: string
                type: object
              ipList:
                description: IPList configuration (for IPAllowList/IPDenyList types)
                properties:
                  cidrs:
                    description: CIDRs is a list of CIDR blocks
                    items:
                      type: string
                    minItems: 1
                    type: array
                  sourceHeader:
                    description: |-
                      SourceHeader specifies an HTTP header to extract the client IP from
                      (e.g., X-Forwarded-For, X-Real-IP)
                    type: string
                required:
                - cidrs
                type: object
              jwt:
                description: JWT configuration (for JWT type)
                properties:
                  audience:
                    description: Audience is the expected JWT audience
                    items:
                      type: string
                    type: array
                  headerName:
                    default: Authorization
                    description: HeaderName is the header containing the JWT token
                    type: string
                  headerPrefix:
                    default: 'Bearer '
                    description: HeaderPrefix is the prefix before the token in the
                      header
                    type: string
                  issuer:
                    description: Issuer is the expected JWT issuer
                    type: string
                  jwksUri:
                    description: JWKSUri is the URL to fetch JWKS for verification
                    type: string
                required:
                - issuer
                - jwksUri
                type: object
              rateLimit:
                description: RateLimit configuration (for RateLimit type)
                properties:
                  burst:
                    description: Burst is the maximum burst size
                    format: int32
                    minimum: 1
                    type: integer
                  key:
                    default: source-ip
                    description: Key determines what to rate limit by (e.g., source
                      IP, header value)
                    type: string
                  requestsPerSecond:
                    description: RequestsPerSecond is the maximum number of requests
                      per second
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - requestsPerSecond
                type: object
              targetRef:
                description: TargetRef identifies the resource this policy applies
                  to
                properties:
                  kind:
                    description: Kind is the kind of resource (e.g., ProxyGateway,
                      ProxyRoute)
                    enum:
                    - ProxyGateway
                    - ProxyRoute
                    - ProxyBackend
                    type: string
                  name:
                    description: Name is the name of the resource
                    type: string
                  namespace:
                    description: Namespace is the namespace of the resource (defaults
                      to policy namespace)
                    type: string
                required:
                - kind
                - name
                type: object
              type:
                description: Type is the type of policy
                enum:
                - RateLimit
                - JWT
                - IPAllowList
                - IPDenyList
                - CORS
                type: string
            required:
            - targetRef
            - type
            type: object
          status:
            description: ProxyPolicyStatus defines the observed state of ProxyPolicy
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the policy's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
