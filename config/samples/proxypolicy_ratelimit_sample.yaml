apiVersion: novaedge.io/v1alpha1
kind: ProxyPolicy
metadata:
  name: api-rate-limit
  namespace: default
spec:
  type: RateLimit
  targetRef:
    kind: ProxyRoute
    name: example-route
    namespace: default
  rateLimit:
    requestsPerSecond: 100
    burst: 200
    key: source-ip
